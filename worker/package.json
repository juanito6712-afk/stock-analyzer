{
  "name": "telegram-tutor-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram webhook entrypoint that hands question-generation off to GitHub Actions",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
